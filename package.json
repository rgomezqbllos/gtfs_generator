{
    "name": "gtfs-generator",
    "version": "2.0.0",
    "description": "GTFS Generator — Herramienta de digitalización de redes de transporte público",
    "scripts": {
        "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
        "start": "npm run dev",
        "build": "npm --prefix client run build && npm --prefix server run build",
        "start:prod": "npm --prefix server run start",
        "osrm:setup": "npm --prefix server run osrm:setup --",
        "install:all": "npm install && npm --prefix client install && npm --prefix server install",
        "review:consolidate": "node ops/review/scripts/consolidate-findings.mjs"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
